# .env.example
# --------------
# 이 파일은 git에 포함됩니다. 실제 값은 .env.local에 작성하세요.
# .env.local은 .gitignore에 등록되어 있어 외부에 노출되지 않습니다.

# FastAPI 백엔드 서버 주소
# NEXT_PUBLIC_ 접두사가 있어야 브라우저(클라이언트)에서 접근 가능
NEXT_PUBLIC_API_URL=board-backend-production-eea2.up.railway.app
